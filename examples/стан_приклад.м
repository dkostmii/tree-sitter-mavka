;; Джерело: https://github.com/mavka-ukr/stan

взяти пак стан [Стан]

стан = Стан((рахунок=1))

стан.слухати((): друк("стан змінено"))

дія збільшити()
  стан["рахунок"] += 1
кінець

збільшити()
збільшити()
збільшити()

друк(стан)
